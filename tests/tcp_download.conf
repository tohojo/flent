## -*- coding: utf-8 -*-

include("tcp_bidirectional.conf", globals())
DESCRIPTION="TCP download stream w/ping"

del DATA_SETS['TCP upload']
PLOTS['totals']['series'] = dict([v for v in PLOTS['totals'] if v['data'] != 'TCP upload'])
