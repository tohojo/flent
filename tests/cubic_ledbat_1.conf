## -*- mode: python; coding: utf-8 -*-

include("netperf_definitions.inc", globals())
DESCRIPTION="Cubic vs LEDBAT upload streams w/ping"
DEFAULTS={'PLOT': 'totals'}

DATA_SETS = o([
        ('TCP cubic',
         {'command': "%s -H %s -t omni -l %d -f m -- -K cubic,cubic" % (GLOBAL_COMMAND, HOST, LENGTH),
          'delay': DELAY,
          'units': 'Mbits/s',
          'runner': 'netperf_demo',}),
        ('TCP ledbat',
         {'command': "%s -H %s -t omni -l %d -f m -- -K ledbat,ledbat" % (GLOBAL_COMMAND, HOST, LENGTH),
          'delay': DELAY,
          'units': 'Mbits/s',
          'runner': 'netperf_demo',}),
        ('Ping (ms) ICMP',
         {'command': '%s -D -i %.2f -w %d %s' % (PING, max(0.2,STEP_SIZE), TOTAL_LENGTH, HOST),
          'units': 'ms',
          'runner': 'ping',}),
        ])


PLOTS = o([
    ('totals',
     {'description': 'Bandwidth and ping plot',
      'type': 'timeseries',
      'dual_axes': True,
      'series': [{'data': 'TCP cubic',
                  'label': 'Cubic'},
                 {'data': 'TCP ledbat',
                  'label': 'LEDBAT'},
                 {'data': 'Ping (ms) ICMP',
                  'label': 'Ping (ms)',
                  'color': 'red',
                  'axis': 2}]}),
    ('ping_cdf',
     {'description': 'Ping CDF plot',
      'type': 'cdf',
      'series': [{'data': 'Ping (ms) ICMP',
                  'label': 'Ping (ms)'},],
      'cutoff': (DELAY,DELAY)}),
    ])
