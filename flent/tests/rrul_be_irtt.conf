## -*- mode: python; coding: utf-8 -*-

include("rrul_be.conf")
DESCRIPTION="Realtime Response Under Load - exclusively Best Effort (irtt RTT)"

for k,v in DATA_SETS.items():
    if v['runner'] == 'netperf_demo' and v['units'] == 'ms':
        v['runner'] = 'irtt'
        v['command'] = find_irtt(STEP_SIZE, TOTAL_LENGTH, HOST, ip_version=IP_VERSION)
        del v['data_transform']
